import { ComingSoon } from '@/components/ui/ComingSoon'

export function OrdersPage() {
  return (
    <ComingSoon
      title="Your orders"
      message="Order history is coming in a later phase — the catalog and checkout ship first."
    />
  )
}
